{
    "name": "myproject",
    "defaultBranch": "master",
    "remote": {
        "host": "drs",
        "directory": "/var/storage/drs",
        "rsyncOptions": {
            "get":"-az --delete-during --checksum",
            "put":"-az --delete-during --whole-file"
        }
    }
}
